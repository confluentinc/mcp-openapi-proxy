spring:
  config:
    activate:
      on-profile: "default"
  application:
    name: "JavaRCSProvider"
  main:
    web-application-type: none
kafka:
  broker:
    url: ${BROKER_URL}
    security_protocol: SASL_SSL
    sasl_mechanism: PLAIN
    jaas_config: org.apache.kafka.common.security.plain.PlainLoginModule required username="${JAAS_USERNAME}" password="${JAAS_PASSWORD}";
  schema-registry:
    url: ${SR_URL}
    auth_info: ${SR_API_KEY}:${SR_API_SECRET}
registration:
  registration-topic: "_agent_registry"
  application-id: "SampleJavaAgent"
  rcs-request-topic: "sample_agent_rcs_request"
  rcs-response-topic: "sample_agent_rcs_response"
topic:
  timeout: 10000
  partitions: 6
  replication-factor: 3